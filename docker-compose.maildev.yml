version: "3.8"

services:
  maildev:
    image: maildev/maildev:latest
    container_name: finwise-maildev
    ports:
      - "1025:1025" # SMTP port
      - "1080:1080" # Web UI port
    environment:
      - MAILDEV_WEB_PORT=1080
      - MAILDEV_SMTP_PORT=1025
    restart: unless-stopped
